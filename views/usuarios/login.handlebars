{{#each erros}}
    <div class="alert alert-danger">{{msg}}</div>
{{else}}

{{/each}}

<h1>Login</h1>
<hr>
<div class="card">
    <div class="card-body">
        <form action="/usuarios/login/success" method="POST" >
            <label for="email" class="mt-2">Email: </label>
            <input type="email" name="email" required class="form-control" placeholder="Seu email">

            <label for="senha" class="mt-2">Senha: </label>
            <input type="password" name="senha" required class="form-control" placeholder="Sua senha">

            <button type="submit" class="btn btn-success mt-2">Entrar</button>
        </form>
    </div>
</div>