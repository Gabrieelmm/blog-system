<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        
        <small>Data de publicação: {{postagem.date}}</small> <br>
        <hr>
        <p>{{postagem.conteudo}}</p>
    </div>
</div>